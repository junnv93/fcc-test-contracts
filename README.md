# fcc-test-contracts

> ## 이 레포는 **정식 분리된 레포**입니다 (2026-09-05 갱신)
>
> 이 레인은 자기 일을 수행합니다 — **자기 OpenAPI 를 스스로 발행하고**(`d83ebee`),
> 공유 계약의 결함도 여기서 고칩니다(`4740cd0`).
>
> ### 배송 기계는 **퇴역했습니다**
>
> 모노레포 커밋 `91d0febe`(2026-08-31, main 계보) — *"공유 레인이 모노레포를 떠난다 …
> 그것을 만들던 **추출·배송 기계와 그 검사 축이 함께 퇴역한다.** 모노레포는 이제 두
> 패키지를 pip 로 소비한다."*
>
> `scripts/stamp_delivery_provenance.py` 와 `packaging/` 이 모노레포에서 **삭제**돼
> 재배송이 물리적으로 불가능하고, **방향이 역전**됐습니다 — 모노레포
> `requirements-*.txt` 가 `fcc-test-contracts@v0.1.12` 를 **소비**합니다.
>
> ### `.extraction-layout.json` 을 어떻게 읽어야 하나
>
> 🔒 는 **「배송이 만든 파일」이라는 출처 기록**이지 **「고치지 마라」가 아닙니다.**
> 그 파일이 필요한 이유는 게이트가 아니라 **런타임 아티팩트 해소기**의 입력이기
> 때문입니다 — **지우지 마십시오.**
>
> ### 정정 이력
>
> 🔴 이 배너의 옛 판(「읽기 전용 납품물」)이 **설계 오류를 낳았습니다.** 그리고 그 뒤
> 「배송이 거부한다」로 고친 판(PR #33)도 **과잉이었습니다** — 배송이 없기 때문입니다.
> 구조·품질 판정은 **이 레포에 직접 도구를 돌려서** 하십시오.

---

## 이 레포는 무엇인가

세 레인이 공유하는 **의존성 없는 웹 경계 커널(dependency-free shared web-boundary kernel)** 입니다.
provider API DTO, 라우트/operation/스키마 계약, 호환성 검사기와 그 export/check 스크립트,
그리고 모든 웹 표면이 HTTP 뒤에 앉기 위해 필요한 원시 요소들 — 접근 정책, 인증 설정과
OIDC principal 해소, correlation 과 trace 컨텍스트 — 을 담습니다.

**이 레인의 서드파티 하드 의존은 0 입니다.** 그것이 이 레인의 P0 이고, 모노레포의 게이트가
별도 인터프리터 실행으로 판정합니다. `PyJWT` 는 `oidc` extra 로만 들어오며, 그것 없이도
패키지는 설치되고 import 됩니다(OIDC principal 해소가 함수 안에서 lazy import 하기 때문).

의존 방향은 **단방향**입니다. 이 레인은 아무에게도 의존하지 않고, `fcc-test-platform` 이
이 레인에 의존합니다.

---

## 받아가기

```bash
git clone https://github.com/junnv93/fcc-test-contracts.git
cd fcc-test-contracts
```

⚠️ **HTTPS 를 쓰세요.** 이 레포들은 private 이고, 배송 머신에서는 SSH 키가 등록돼 있지
않습니다(실측 2026-08-30: `Permission denied (publickey)`). `gh auth login` 후
`gh auth setup-git` 을 한 번 돌리면 HTTPS 자격증명이 붙습니다.

---

## 지금 이 사본이 정확히 무엇인가 → `EXTRACTED_FROM.md`

원본 SHA · 추출 시각 · 매니페스트 버전 · 파일 수 · 알려진 실패 수는 **`EXTRACTED_FROM.md`** 에
있고 배송할 때마다 새로 생성됩니다.

⚠️ **이 README 에는 그 숫자를 적지 않습니다.** 배송마다 바뀌는 값을 두 곳에 적으면
반드시 어긋나고, 어긋난 쪽을 읽은 사람은 틀린 것을 믿게 됩니다. 숫자가 필요하면
`EXTRACTED_FROM.md` 를 보세요.

---

## ⚠️ 처음 clone 한 사람이 먼저 알아야 할 것 셋

<!-- 정정 이력 (2026-09-07) — 지우지 마세요.
     이 절의 옛 판은 세 항목 «전부» 가 오늘 거짓이었습니다:
       ①「여기서 고치지 마세요」        → 위 배너가 정반대를 말한다(배송 기계 퇴역)
       ②「git history 가 없습니다」      → 실측 144 커밋 (2026-08-30 ~ )
       ③「모노레포 매니페스트에 등재」    → 이 레포의 delivered_test_run_baseline.json 에 있다
     배너는 PR #33 에서 정정됐는데 «본문이 따라오지 않아» 배너와 서로 모순인 채로 남았습니다.
     옛 판을 믿고 「여기서는 못 고친다」고 판단한 사람이 나오는 것을 막기 위해 기록을 남깁니다. -->

### 1. 여기서 고칩니다 — 이 레인은 «스스로 사는» 저장소입니다

**배송 기계는 2026-08-31 에 퇴역했습니다**(위 배너). 재배송이 물리적으로 불가능하므로
**여기서 고친 것이 덮어써지지 않습니다.** 설계·품질 판정도 **이 레포에 직접 도구를
돌려서** 하십시오 — 모노레포의 수치를 인용하지 마십시오.

> 🔴 **옛 판은 「여기서 고치지 마세요 (위 §읽기 전용)」였습니다.** 배너가 그 전제를
> 이미 버렸는데 이 줄만 남아, **배너와 본문이 서로 반대를 말하고 있었습니다.**

### 2. git history 가 있습니다 — 다만 «첫 배송부터» 입니다

```bash
git rev-list --count HEAD      # 실측 2026-09-07: 144
git log --reverse --oneline | head -1
#   063ca9a chore: 모노레포 추출 첫 배송   (2026-08-30)
```

**첫 커밋 이전의 history 는 없습니다** — 운영자 판정으로 이전하지 않았습니다.
그러므로:

* **2026-08-30 «이후»의 「누가 왜 이 줄을 썼나」** → `git log` / `git blame` 이 답합니다.
* **그 이전** → 모노레포(`FCC_mobile_test_automation`, **private**)를 조회하십시오.

> 🔴 **옛 판은 「git history 가 없습니다 … 첫 커밋이 이 레포의 전부입니다」였습니다.**
> 배송 첫날에는 참이었고, 그 뒤 **143 커밋이 쌓이면서** 거짓이 됐습니다.

### 3. 테스트가 전부 통과하지 않습니다 — 선언은 «이 레포» 안에 있습니다

이 레인은 자기 테스트 suite 를 싣고 있고, **일부는 실패한 채입니다.**
그 실패는 **node id 집합으로 이 레포의 `delivered_test_run_baseline.json` 에 선언**돼
있고, 한 방향으로만 줄어듭니다.

```bash
python3 -c "import json;d=json.load(open('delivered_test_run_baseline.json'));print(d['lane'],len(d['baseline']))"
#   fcc-test-contracts 29        (실측 2026-09-07)
```

> **당신이 무언가를 깨뜨린 것이 아닙니다.** 선언과 관측이 어긋날 때만 문제입니다.
>
> 🔴 **옛 판은 「모노레포 매니페스트에 등재돼 있고(`governance.delivered_test_run_baseline`)」
> 였습니다.** 그 매니페스트를 만들던 배송 기계가 퇴역했으므로, 오늘 선언의 SSOT 는
> **이 파일** 하나입니다.

⚠️ 개수가 아니라 **이름 집합**으로 판정하는 이유: 개수는 *고쳐진 실패*와 *새로 깨진 실패*를
맞바꾼 것을 구분하지 못합니다. 통과 개수가 그대로여도 다른 것이 깨져 있을 수 있습니다.

---

## 어떻게 돌리나

### 설치

```bash
python3 -m pip install -e .            # 런타임 (서드파티 의존 0)
python3 -m pip install -e '.[test]'    # + pytest
python3 -m pip install -e '.[oidc]'    # + PyJWT — auth_mode=oidc_jwt 배포에서만 필요
```

Python **3.11 이상**이 필요합니다(중앙 API 컨테이너 베이스 이미지에서 파생된 하한).

### 테스트

이 레인은 형제 레인이 없으므로 상자 하나로 돕니다.

```bash
PYTHONPATH="$PWD:$PWD/scripts" \
python3 -m pytest -q -p no:randomly -p no:cacheprovider \
        --tb=no -ra --continue-on-collection-errors
```

⚠️ **`--continue-on-collection-errors` 는 장식이 아닙니다.** pytest 는 기본적으로 모듈 하나가
import 실패하면 수집 전체를 중단합니다. 그러면 상자가 **0개를 수집하고**, 0개는 "새 실패 없음"을
완벽하게 만족합니다 — 실패가 성공과 같은 모양이 됩니다. 이 플래그가 그 모듈을 하나의 `ERROR`
노드로 남겨서 다른 실패와 똑같이 판정되게 합니다.

⚠️ **`scripts/` 는 이 상자에서 의도적으로 패키지가 아닙니다**(`__init__.py` 없음).
그 모듈들은 최상위 이름으로만 도달 가능하므로 `PYTHONPATH` 에 `scripts/` 가 따로 들어갑니다.
그래서 `pip install` 은 `scripts/` 를 담지 않습니다.

---

## ⚠️ 세션 노드 배포 자산은 이 상자에 없습니다

이 상자는 `packages/` 를 싣지만 `packages/session-node-artifacts/` 는 **없습니다.**
그것은 매니페스트에 실재하는 별도 레인 `fcc-chamber-node` 의 소유이고, 그 레인은 오늘
**어느 상자에도 배송되지 않습니다**(`extraction_target: false`).

`packages/` 가 있는데 그 하위 하나만 없으므로 찾다 보면 빠뜨린 것처럼 보입니다.
빠뜨린 것이 아닙니다. `fcc-chamber-node` 가 배송되기 시작하면 이 문단을 요구하는 봉인이
스스로 꺼집니다.

---

## 무엇이 아직 없나 — 이 배송의 완료명은 「첫 배송 + 리허설」입니다

「레포 분리 완료」가 **아닙니다.** 다음이 아직 없습니다:

| 없는 것 | 뜻 |
|---|---|
| **CI** | 이 레포에서 게이트가 돌지 않습니다. 판정은 아직 모노레포에서만 납니다 |
| **lockfile** | 두 배포물을 담을 인덱스가 아직 없어 해석기가 비교할 대상이 없습니다 |
| ~~**설치 검증**~~ | ✅ **했습니다 (2026-08-31)** — 갓 clone 한 트리에서 `pip install "fcc-test-contracts @ git+…@v0.1.2"` 가 성공하고, platform 은 `BcryptPasswordHasher` 왕복까지 확인했습니다. ⚠️ *휠이 빌드된다*와 *설치된 배포물이 돈다*가 다른 명제라는 이 행의 말은 옳았고, 그래서 재 봤습니다 |
| ~~**태그된 릴리스**~~ | ✅ **있습니다 (2026-08-30~31)** — `v0.1.0` · `v0.1.1` · `v0.1.2`. `requirements` 와 형제 핀은 태그를 가리키며, 그 태그가 선언 `version` 과 같은지는 드리프트 게이트가 봅니다 |

---

## 검사를 켜 두기 — `pre-push` 훅 (clone 마다 한 번)

이 상자에는 검사가 하나 있고, **당신이 켜야 켜집니다.**

```sh
git config core.hooksPath githooks
```

⚠️ **왜 GitHub 이 대신 해 주지 않나.** 2026-08-30 실측: 이 계정의 GitHub Actions 는
잡을 **러너에 배정하지 못합니다**. 본문이 `echo` 한 줄뿐인 워크플로조차 2초 만에
`steps: []` · `runner_name: ""` 로 실패합니다 — 즉 *검사가 실패한 것*이 아니라
*시작조차 못 한 것*이고, 두 상태는 화면에서 똑같은 빨간 X 로 보입니다.
`.github/workflows/checks.yml` 은 그래서 오늘 **휴면**입니다. 지우지 마세요 —
결제/가시성이 풀리는 날 같은 검사를 그대로 이어받습니다.

### 이 검사가 판정하는 것

**전부 통과인가**가 아닙니다. 이 상자는 오늘 전부 통과하지 못합니다(모노레포에만
있는 경로를 단언하는 테스트가 남아 있고, 분리가 끝나야 사라집니다). 대신
**관측된 실패의 이름 집합이 `delivered_test_run_baseline.json` 의 선언과 같은가**를
봅니다. 그래서 셋이 동시에 성립합니다:

* 알려진 실패는 **통과**로 읽힙니다 — 당신을 헛되이 막지 않습니다.
* 새로 깨진 것은 **즉시 red** 이고 **이름으로** 말합니다.
* 고쳐진 것도 red 입니다 — 선언이 낡았다는 뜻이고, 그것도 소식입니다.

⚠️ **개수가 아니라 이름 집합입니다.** 하나 고치고 하나 깨뜨리면 개수는 같습니다.

직접 돌리려면:

```sh
python3 scripts/lane_check.py
```

### ⚠️ 판정이 설치 방식에 따라 달라집니다

`pip install .`(non-editable)은 상자 안에 `build/lib/` 를 만들고, 트리를 스캔하는
테스트 일부가 그 사본을 원본과 **함께 셉니다** — 선언에 없는 실패가 생깁니다.
그리고 `.gitignore` 가 `build/` 를 덮으므로 **`git status` 는 깨끗하다고 답합니다.**
게이트는 그런 트리에서 판정을 **거부**하고 무엇을 지우라고 말합니다.

판정에 쓰는 설치는 이것입니다:

```sh
pip install -e '.[test,oidc]'
```

### 한계 — 이것은 실수 방지층이지 방어층이 아닙니다

설치가 clone 마다 opt-in 이고, `--no-verify` 한 번이면 사라집니다.
**그래서 훅은 «로컬에서 빨리 알려 주는 층»이지 마지막 방어선이 아닙니다** —
마지막 방어선은 서버의 branch protection 입니다.

<!-- 정정 이력 — 지우지 마세요. -->
> 🔴 **정정 (2026-09-07 12:08 실측).** 이 문단의 옛 판은 이랬습니다:
>
> > *"진짜 강제는 러너가 돌아오고 branch protection 이 이 검사를 required 로 거는
> > **날에 생깁니다.** 그날까지 이것이 **가장 값싼 근사**입니다."*
>
> **그날은 이미 왔습니다.** 이 레포의 `main` 은 `lane-check` 를 **required** 로 겁니다:
>
> ```bash
> gh api repos/junnv93/fcc-test-contracts/branches/main/protection
> # → required_status_checks.contexts = ["lane-check"]
> ```
>
> ✅ **그리고 그 방어선에는 예외가 없습니다** — `enforce_admins: true`
> (2026-09-07 13:4x 부터. 소비 레인 `fcc-test-platform` 과 같습니다).
> **관리자도 우회하지 못합니다.**
>
> 🔄 **정정 (같은 날).** 이 자리의 앞 판은 *「⚠️ 다만 이 레인에는 구멍이 하나 남아
> 있습니다 — `enforce_admins: false` … 「서버가 마지막 방어선이다」는 **협업자에
> 대해서만** 참」* 이었습니다. 그 값이 오후에 뒤집혔습니다 — 근거의 전제(러너 할당량
> 미해소)가 운영자 판정으로 해소됐기 때문입니다. 되돌리는 조건은
> `CLAUDE.md` §`enforce_admins` 절에 있습니다.
>
> ⚠️ **같은 문장이 소비 레인에도 있었습니다** — `fcc-test-platform/README.md` 의
> 같은 절에 글자 그대로 같은 문단이 있었고 같은 날 함께 고쳤습니다.
> **한 사실이 거짓이 되면, 그 사실을 말하는 자리가 «레포 경계를 넘어» 있을 수
> 있습니다.**

우회해야 하면 `FCC_SKIP_LANE_CHECK=1 git push`, 그리고 **왜 우회했는지 적으세요.**

## 변경은 어디로 보내나 — **여기로 보냅니다**

<!-- 정정 (2026-09-07) — 지우지 마세요.
     이 절의 옛 판은 「납품 파일을 고치면 «배송이 멈춘다» · 모노레포에서 고쳐라」였습니다.
     ⚠️ 위 배너가 이미 그 판을 «과잉»이라고 적었는데(PR #33 정정) 이 절이 따라오지 않아,
        배너와 본문이 서로 반대를 말하고 있었습니다. 배송 기계가 없으므로 멈출 배송도 없습니다. -->

**이 레인에서 고치고, 이 레인에 PR 을 냅니다.** 배송 기계가 퇴역했으므로(위 배너)
여기서 고친 것이 덮어써지지 않고, **고칠 다른 곳도 없습니다.**

| 하려는 일 | 어디서 |
|---|---|
| 새 파일·새 모듈을 더한다 | **여기** |
| 기존 파일(🔒 표시 포함)을 고친다 | **여기** — 🔒 는 출처 기록이지 「고치지 마라」가 아닙니다 |
| 계약을 바꾼다 | **여기.** 그리고 **태그를 내야** 소비 레인이 그것을 봅니다 |
| 「누가 왜 이 줄을 썼나」 (2026-08-30 이전) | 모노레포 조회 (**private** — 별도 권한) |

⚠️ **계약을 바꾸는 것은 «2-레포 작업»이고 순서가 있습니다:**

```
① 여기서 고친다  →  ② 태그를 «낸다»  →  ③ 소비 레인의 핀을 올린다  →  ④ 소비 확인
```

**핀은 「태그」입니다. 브랜치가 아닙니다.** ②를 건너뛰면 소비 레인은 그 변경을
**보지 못합니다.**

⚠️ **그리고 어떤 결함은 «소비 레인에서만» 보입니다.** 여기 전량이 초록이어도
`fcc-test-platform` 에서 깨질 수 있고, 그 갈라짐은 **양방향**입니다.
태그를 내기 전에 **핀만으로 만든 환경**에서 재십시오.

⚠️ **비공개 경계는 그대로입니다** — 측정 코드·GUI·EUT 제어·Excel 은 provider 레포에
남습니다. 그것이 이 분리의 목적이고, **그 경계는 「여기서 고치지 마라」와 무관합니다.**

## 레이아웃

| 경로 | 내용 |
|---|---|
| `fcc_test_contracts/` | 레인의 런타임 코드. 최상위 import 패키지 |
| `tests/` | 이 레인의 테스트 suite (귀속 ∪ import 폐포 ∪ `conftest.py`) |
| `scripts/` | 운영자 CLI. **패키지가 아닙니다** — 최상위 이름으로 도달 |
| `artifacts/` · `packages/` | 계약 아티팩트(OpenAPI 스키마 등)와 npm 배포물 |
| `docs/` | 이 레인이 소유한 계약 문서 |
| `pyproject.toml` | 설치 선언. **여기서 고칩니다** — 배송 기계 퇴역(2026-08-31). ⚠️ `dependencies` 가 `[]` 인 것은 설계입니다: `test_contracts_lane_still_declares_no_dependencies` 가 봉인합니다 |
| `.extraction-layout.json` | ⚠️ **지우지 마세요.** 패키저가 *무엇을 어디로 옮겼는지* 남긴 기록이고, 런타임 아티팩트 해소기가 이것을 읽습니다. 빌드 잔여물이 아닙니다 |
