# 설치 선언 — fcc-test-kernel 배포판
#
# 이 저장소가 내는 **세 번째 배포물**이다. 루트 `pyproject.toml` 이 Python
# `fcc-test-contracts` 를, `packages/api-artifacts/package.json` 이 npm
# `@fcc/api-artifacts` 를 낸다. 「한 저장소, 여러 배포판」은 이 계열의 확립된
# 패턴이고, 이 배포판이 새 저장소를 필요로 하지 않는 이유가 그것이다.
#
# ⚠️ **이것은 계약 레인이 아니다.** `fcc-test-contracts` 는 provider 어휘를 받지
# 않는다(ADR-0010 D-8). 공유 커널 폐포 53개 중 9개가 코드에 provider 어휘를 지므로
# 그 레인으로 승격할 수 없다. 그래서 **별도 배포판 · 고유 접두사**다 —
# `fcc-test-platform:docs/architecture/adr/0001-shared-kernel-prefix-and-name-ownership.md`.
#
# 왜 접두사인가: 두 레인이 최상위 이름 넷(`domain`·`application`·`infrastructure`·
# `logger_config`)을 동시에 주장하고, Python 은 그것을 `sys.path` 순서로 조용히
# 고른다. 공식 지침(packaging.python.org, *Packaging namespace packages*)이 허용하는
# 둘 중 PEP 420 namespace 는 **성립하지 않는다** — 그 요건이 *"모든 배포판이
# `__init__.py` 를 생략"* 인데 두 레인이 양쪽 다 갖고 있다. 남는 것이 「고유 접두사」다.

[build-system]
# 루트 `pyproject.toml` 과 같은 하한. `[tool.setuptools]` 하위 테이블을 함께 쓴다.
requires = ["setuptools>=62.5"]
build-backend = "setuptools.build_meta"

[project]
name = "fcc-test-kernel"
# ⚠️ **버전 SSOT 를 발명하지 않는다.** 이 배포판은 자기 릴리스 축을 갖고, 그 축은
# 이관이 진행되며 폐포가 줄어드는 것과 함께 움직인다.
#   0.1.0  1단계 — 로깅/알림 클러스터 10파일 (공유 폐포 64 → 54)
#   0.2.0  2단계 — 중앙 계약 표면 클러스터 31파일 (54 → 24)
#   0.3.0  3단계 — 남은 공유 폐포 전부 24파일 (24 → 0)
#          3a infrastructure 2 + 3b domain 22. 두 클러스터가 각각 이미 닫혀
#          있었다(유출 0) — 서로 독립이라 한 웨이브로 갈 수 있었다.
#          ⚠️ 이 배포로 소비 레인의 공유 폐포가 **0** 이 된다. 그 순간부터
#          「공유 폐포 0」과 「측정이 아무것도 못 찾았다」가 그 게이트의 축에서
#          같은 값이 되므로, 소비 레인이 그 자리에 비-공허성 팔을 세운다.
#          application 20(central_contract 19 + session/api_contracts) + domain 11.
#          ⚠️ 이 클러스터는 provider 어휘 4를 진다. **그것이 이 배포판이 계약
#          레인이 아닌 이유다** — ADR-0010 D-8 은 계약 레인의 provider 어휘를
#          금지하지만 커널은 접두사로 충돌만 없애므로 어휘를 묻지 않는다
#          (fcc-test-platform ADR-0001 §그리고 9개를 분류할 필요가 없다).
#   0.5.4  🔴 **파손을 담는다** — `DbCursor` 가 실물 DB-API 드라이버를 탈락시키고
#          있었다. `execute` 반환 `None`→`object`(PEP 249 는 정의하지 않는다) ·
#          인자를 위치 전용으로 · `rowcount` 를 읽기 전용 property 로.
#          소비 레인이 `cursor.execute(statement=…, parameters=…)` 처럼 «키워드»로
#          부르고 있으면 그날 빨개진다(fcc-test-platform 전수 0건 확인, 다른
#          소비자는 세지 않았다). 함께: `OperationSpec` 이 런타임에 자기를 잘못
#          말하던 것(`from __future__ import annotations` 때문에 `NotRequired` 가
#          안 보여 `__required_keys__` 가 여섯 전부였다 — 실측 0/80 이 그 집합을
#          채웠다). mypy 는 옳게 봤으므로 정적 축은 침묵하고 있었다.
#
#          ⚠️ **0.5.2 도 0.5.3 도 건너뛴 것이 아니다.** 이 브랜치가 열려 있는 동안
#          형제 배송 둘이 그 번호를 차례로 가져갔다 — PR #48(`93fbf12`) ·
#          PR #51(`e8076bf`). 이 브랜치는 그때마다 다음 번호로 물러났고,
#          `kernel-v0.5.4` 는 그 둘의 «후손»이라 내용을 모두 담는다.
#
#          🔴 **정정 (2026-09-07).** 이 자리에 처음 적은 문장은 #51 을
#          *「선언만, 태그 아직 없음」* 이라고 했다. **쓸 때는 참이었고 지금은
#          거짓이다** — `kernel-v0.5.3` 이 그날 08:18 에 `70a9b30` 에 붙었다.
#          ⚠️ 판정은 그대로 옳다. 0.5.4 를 택한 이유는 「0.5.3 이 없어서」가 아니라
#          **「0.5.3 이 이 판을 안 담아서」**이고, 그것은 태그가 생겨도 변하지 않는다.
#          이런 문장은 아무 게이트도 검사하지 않아 사실이 움직여도 조용하다.
#
#          🔴 **그리고 루트 `pyproject.toml` 이 기록한 0.1.19 사고가 여기서 실제로
#          재현됐다** (2026-09-07 실측). 재기저할 때 `version = "0.5.3"` 줄은 양쪽이
#          «같은 값»을 써서 **충돌 없이 병합됐고**, 충돌한 것은 이 주석 블록뿐이다.
#          주석이 없었다면 git 은 완전히 조용했을 것이다. **버전 충돌은 병합 충돌과
#          모양이 다르다** — 그것이 `test_release_version_is_unclaimed` 가 존재하는
#          이유이고, 그 봉인은 «커밋 뒤»에만 답한다.
#          ⚠️ 그 사고의 «절반»은 실제로 일어났다: 저쪽이 `kernel-v0.5.3` 을 냈고 그
#          태그에 이 판의 변경은 **들어 있지 않다**(이 판이 나중에 머지됐다). 무해한
#          이유는 하나뿐이다 — 이쪽이 그 번호를 비켜 `0.5.4` 로 갔기 때문이다.
#          둘 다 0.5.3 을 썼다면 먼저 태그를 낸 쪽이 이겼을 것이다.
#   0.5.5  주석 정정뿐이다 — 위 「0.5.3 은 태그가 없다」가 거짓이 됐다.
#          ⚠️ **아직 태그가 없다.** 소비 레인이 이것을 필요로 하지 않는다(코드
#          무변경). 다음에 이 레인이 태그를 낼 때 이 번호가 나간다 — 루트
#          `pyproject.toml` 의 `0.1.21` 이 같은 방식으로 처리됐다.
#          ⚠️ 그래도 «번호는» 올려야 한다: `test_no_distribution_reuses_a_tag_that_
#          lacks_its_changes` 는 배포판 파일이 바뀌었는지만 보고 주석인지 코드인지
#          묻지 않는다(실측 — 이 정정만으로 빨개졌다). 그것이 옳다. `kernel-v0.5.4`
#          로 설치한 사람이 받는 트리와 이 트리가 더는 같지 않기 때문이다.
version = "0.5.5"
description = "Prefixed shared kernel for the FCC lanes — logging and notification primitives that both the central platform and the provider runtime reach."
# requires-python = 두 소비 레인의 런타임 베이스(`python:3.11-slim`)에서 파생.
requires-python = ">=3.11"
# 실측 2026-09-03: 이 클러스터 10파일의 클러스터 밖 의존은 `fcc_test_contracts`
# 하나뿐이다(서드파티 0). 그 하나를 여기 선언한다 — 소비 레인이 이미 그것을
# 설치하지만, 이 배포판이 그것 없이 import 되지 않으므로 선언이 사실이어야 한다.
dependencies = [
    "fcc-test-contracts",
]

[tool.setuptools.packages.find]
# 최상위 import 이름 **하나**. 그것이 이 배포판이 존재하는 이유다 —
# 두 레인이 주장하던 네 이름 대신 아무도 다투지 않는 이름 하나를 쓴다.
include = ["fcc_test_kernel*"]
# PEP 420 암묵 네임스페이스. 계약 레인(`fcc_test_contracts`)과 같은 규약이고,
# 원본 트리의 `__init__.py` 를 **가져오지 않은** 것이 여기 걸린다:
# `domain/models/__init__.py` 는 이 클러스터 밖 모듈(`test_plan` ·
# `notification_types`)을 import 하므로, 그것을 함께 실으면 폐포가 조용히 커진다.
# 실측: 그 `__init__` 들 중 이 클러스터를 부르는 것은 0건이라 빼도 아무것도 잃지 않는다.
namespaces = true

[tool.setuptools.package-data]
# ⚠️ **패키지 데이터는 폐포가 볼 수 없는 의존이다** (실측 2026-09-03).
# `domain/models/reference_catalog.py` 는 import 시점에
# `importlib.resources.files(__package__).joinpath('decision_catalogue.json')` 로
# 이 파일을 읽는다. 폐포 게이트는 **import 축**이라 그 간선을 구조적으로 못 본다 —
# 2단계 이관에서 코드 31파일만 옮기자 15개 모듈이 `ReferenceCatalogError:
# decision catalogue resource is unavailable` 로 죽었다.
#
# 축을 하나 더 만드는 대신 **더 강한 오라클**을 세웠다:
# `tests/test_kernel_imports_standalone.py` 가 커널의 모든 모듈을 실제로 import 한다.
# 그 검사는 자원 누락·접두사 오류·패키징 누락을 한 번에 붉힌다.
"fcc_test_kernel.domain.models" = ["*.json"]
# ⚠️ PEP 561 마커. **이것이 없으면 이 배포판의 타입 선언은 소비 레인에서 «투명»하다** —
# mypy 가 설치된 패키지를 미타입으로 보고 import 전체를 `Any` 로 만든다. 실측 2026-09-06:
# platform 에서 `OperationSpec` 을 첨자로 읽어도 잘못된 대입·없는 키가 **0건** 잡혔고,
# `ignore_missing_imports = True` 때문에 `[import-untyped]` 경고조차 안 떴다.
# 즉 «아무 신호 없이 0건 검사»된다. 같은 venv 의 fastapi·psycopg·jwt 는 전부 이것을 싣는다 —
# 서드파티는 싣고 형제 레인만 안 싣고 있었다.
"fcc_test_kernel" = ["py.typed"]
